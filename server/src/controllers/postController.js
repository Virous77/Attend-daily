import { uploadImage, deleteImages } from "../utils/imageUpload.js";
import { createError, handleCallback, sendResponse } from "../utils/utils.js";

export const createPost = handleCallback(async (req, res, next) => {
  console.log("cool");
});
